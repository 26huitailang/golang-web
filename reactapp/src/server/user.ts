import {post} from './request';

interface LoginData {
    username: string
    password: string
}

// export const login(data: LoginData) => {
//     post('/login', data).then(()=> {
//
//     })
// }
